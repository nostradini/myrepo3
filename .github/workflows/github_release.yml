name: Deploy

on:
  # push:
  #   branches: [main]
  workflow_dispatch:
env:
  ENV_TOKEN: ${{ secrets.ACCESS_PAT }}
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Checkout Code

      - name: Call SH
        run: |
        chmod +x ./script/gh_release.sh
        ./script/gh_release.sh $ENV_TOKEN